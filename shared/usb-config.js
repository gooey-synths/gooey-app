const USB_VID = 0xcafe;
const USB_PID = 0x4002;
const BAUD_RATE = 115200;
const EOF_TERMINATOR = 0x1a;
const CHUNK_SIZE = 255;

module.exports = { USB_VID, USB_PID, BAUD_RATE, EOF_TERMINATOR, CHUNK_SIZE };
